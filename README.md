# 🩺 Medical AI Analyzer: Интеллектуальный мониторинг рынка медтехники

## Описание задачи (Business Case)

Ручной анализ сайтов конкурентов в сфере медицинского оборудования — крайне трудоемкий процесс, требующий значительных временных затрат. Маркетологи и бизнес-аналитики вынуждены вручную собирать информацию о ценах, моделях оборудования, оценивать юзабилити и выявлять барьеры для конверсии на каждом сайте конкурента. Это замедляет процесс принятия стратегических решений и снижает оперативность реагирования на изменения рынка.

**Medical AI Analyzer** решает эту проблему через связку **Automation + AI**: автоматизированный сбор визуальных данных с последующим интеллектуальным анализом с помощью GPT-4o Vision. Система позволяет за считанные минуты получить структурированный конкурентный анализ, который ранее требовал часов ручной работы.

## Основной функционал

### 1. Визуальный аудит сайтов с помощью GPT-4o Vision
Система автоматически делает скриншоты целевых страниц конкурентов и передает их в GPT-4o-mini Vision для глубокого визуального анализа. ИИ оценивает дизайн, читаемость, наличие CTA-элементов, контрастность и общую эстетику интерфейса.

### 2. Автоматический сбор (парсинг) цен и характеристик моделей
Извлечение технических спецификаций и ценовой информации из визуального контента с автоматической структуризацией в плоский словарь формата `{"Модель": "Цена/Характеристика"}`.

### 3. Оценка юзабилити (UX/UI) и выявление барьеров покупки
Прецизионный анализ пользовательского опыта с фокусом на реальные проблемы конверсии: наличие кнопок заказа, прозрачность ценообразования, навигация, фильтры и другие элементы, влияющие на решение о покупке.

### 4. Массовый Batch-анализ списка конкурентов из конфига
Автоматизированная обработка списка URL конкурентов, указанных в `config.py`. Система последовательно анализирует каждый сайт и сохраняет результаты в отдельные JSON-файлы с временными метками.

### 5. Сохранение истории отчетов в JSON
Все результаты анализа сохраняются в директории `history/` в структурированном JSON-формате с метаданными (timestamp, URL, путь к скриншоту), что позволяет отслеживать изменения конкурентной среды во времени.

## Технологический стек

- **FastAPI** — современный веб-фреймворк для создания REST API с автоматической документацией
- **Selenium WebDriver** — автоматизация браузера в headless-режиме для захвата скриншотов
- **OpenAI API (GPT-4o-mini Vision)** — мультимодальная модель для визуального анализа и структурированного вывода
- **Pydantic** — валидация данных и типобезопасность через модели
- **BeautifulSoup4** — парсинг HTML-контента для текстового анализа
- **python-dotenv** — управление конфигурацией через переменные окружения

## AI Pipeline (Архитектура)

Система реализует **Vision-пайплайн** с применением техники **Chain of Verification** для защиты от галлюцинаций:

```
1. URL → Selenium WebDriver (Headless Mode)
   ↓
2. Захват скриншота страницы (1920x1080, ожидание загрузки 5 сек)
   ↓
3. Base64 Encoding изображения
   ↓
4. GPT-4o-mini Vision Analysis
   ├─ Визуальная верификация элементов (CTA, цены, контрастность)
   ├─ Извлечение технических спецификаций
   ├─ Оценка UX/UI с пояснениями
   └─ Структурированный JSON Output
   ↓
5. Валидация через Pydantic (AnalysisResult)
   ↓
6. Сохранение в history/ с метаданными
```

**Защита от галлюцинаций**: Промпты содержат строгие инструкции по визуальной верификации, запрещающие выдумывать недостатки или использовать шаблоны. ИИ обязан признавать наличие элементов, если они присутствуют на изображении.

## Пример результата

Ниже представлен пример структурированного JSON-ответа на основе модели `AnalysisResult`:
![Результат анализа конкурента](result_example.png)

```json
{
  "company_name": "DealMed",
  "product_category": "Неврологический инструментарий",
  "strengths": [
    "Наличие акцентных кнопок заказа в каждой карточке товара",
    "Четкая индикация брендов производителей (3M Littmann, Riester)",
    "Наглядная сетка товаров с прозрачным ценообразованием",
    "Присутствие фильтров по категориям в боковой панели",
    "Профессиональная типографика и читаемость текста"
  ],
  "weaknesses": [
    "Водяные знаки мешают детальному осмотру товара",
    "Отсутствие фильтров по ценовому диапазону",
    "Избыточное пустое пространство в шапке сайта",
    "Нет индикации наличия товара на складе в карточках"
  ],
  "technical_specs": {
    "Стетоскоп 3M Littmann Classic III": "12,500 руб.",
    "Стетоскоп Riester Ri-Rap 2.0": "8,900 руб.",
    "Стетоскоп Omron Sprague Rappaport": "6,200 руб.",
    "Стетоскоп детский Little Doctor LD-100": "2,800 руб."
  },
  "design_score": "8/10 (Баллы сняты за навязчивые водяные знаки, в остальном UX и контрастность на высоком уровне)",
  "market_potential": "7/10 (Высокий потенциал за счет узнаваемых брендов и прозрачного ценообразования)",
  "overall_strategy": "Стратегия: Экспертный дистрибьютор. Фокус на премиальные бренды и ассортимент. Основной барьер для клиента — водяные знаки, затрудняющие визуальную оценку товара"
}
```

## Инструкция по запуску

### 1. Создание виртуального окружения

Рекомендуется использовать виртуальное окружение для изоляции зависимостей проекта:

```bash
python -m venv venv
```

**Активация виртуального окружения:**
- Windows: `venv\Scripts\activate`
- Linux/macOS: `source venv/bin/activate`

### 2. Установка зависимостей

Установите все необходимые зависимости одной командой:

```bash
pip install -r requirements.txt
```

### 3. Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```env
AI_KEY=your_openai_api_key_here
AI_URL=https://api.openai.com/v1
```

### 4. Запуск сервера

```bash
uvicorn main:app --reload --host 127.0.0.1 --port 8000
```

После запуска API будет доступен по адресу `http://127.0.0.1:8000`. Интерактивная документация Swagger UI доступна по адресу `http://127.0.0.1:8000/docs`.

### 5. Использование API

**Ручной анализ одного сайта:**
```bash
POST http://127.0.0.1:8000/analyze-site
Body: "url": "https://example.com"
```

**Массовый анализ всех конкурентов из config.py:**
```bash
POST http://127.0.0.1:8000/analyze-all-competitors
```

**Анализ текста:**
```bash
POST http://127.0.0.1:8000/analyze-text
Body: "text": "Ваш текст для анализа"
```

## Структура проекта

```
medical-ai-analyzer/
├── main.py                 # FastAPI приложение и эндпоинты
├── services.py             # OpenAIService (GPT-4o Vision интеграция)
├── parsing_service.py      # ParsingService (Selenium скриншоты)
├── models.py               # Pydantic модели (AnalysisResult)
├── config.py               # Настройки и список конкурентов
├── requirements.txt        # Список зависимостей проекта
├── .env                    # Переменные окружения (не в git)
├── screenshots/            # Автоматически создаваемая папка для скриншотов
├── history/                # Автоматически создаваемая папка для отчетов
└── README.md               # Документация
```

## Особенности реализации

- **Headless Selenium**: Автоматизация браузера без графического интерфейса для работы на серверах
- **Структурированный вывод**: Использование `response_format={ "type": "json_object" }` для гарантированного JSON-формата ответа
- **Валидация данных**: Pydantic модели обеспечивают типобезопасность и автоматическую проверку структуры
- **Chain of Verification**: Детальные промпты с инструкциями по визуальной верификации для минимизации ошибок ИИ
- **Batch-обработка**: Автоматическая обработка списка URL с обработкой ошибок для каждого сайта

---

**Разработано для автоматизации конкурентной разведки в медицинской индустрии** 🚀

## Лицензия

MIT
